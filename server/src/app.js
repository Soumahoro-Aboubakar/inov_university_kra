import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import morgan from 'morgan'; import 'express-async-errors';
import { env } from './config/env.js'; import { errorHandler, notFound } from './middleware/errors.js';
import auth from './routes/auth.js'; import catalog from './routes/catalog.js'; import users from './routes/users.js'; import schedules from './routes/schedules.js'; import dashboard from './routes/dashboard.js'; import exportsRoute from './routes/exports.js'; import discussions from './routes/discussions.js'; import communications from './routes/communications.js'; import contractors from './routes/contractors.js'; import notifications from './routes/notifications.js';
const app = express(); app.use(helmet({ crossOriginResourcePolicy: false })); app.use(cors({ origin: env.clientOrigin })); app.use(express.json({ limit: '1mb' })); app.use(morgan('dev'));
app.get('/api/health', (req, res) => res.json({ status: 'ok' })); app.use('/api/auth', auth); app.use('/api/catalog', catalog); app.use('/api/users', users); app.use('/api/schedules', schedules); app.use('/api/dashboard', dashboard); app.use('/api/exports', exportsRoute); app.use('/api/discussions', discussions); app.use('/api/communications', communications); app.use('/api/contractors', contractors); app.use('/api/notifications', notifications); app.use(notFound); app.use(errorHandler); export default app;
